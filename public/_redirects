# The portfolio is one bundle on hash routes, so an unknown path has to be
# served the shell rather than a bare 404. Static files win over this rule, so
# /gefängnisplanet/ and /prisonplanet/ are answered by their own documents
# before it is ever reached.
/*  /index.html  200
